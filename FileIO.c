/* #include <stdio.h>

int main()
{
    FILE *ptr;
    ptr = fopen("m.txt1", "r"); // For reading the file
    int null;
    if (ptr == null)
    {
        printf("This does not exist");
    }
    else
    {
        int st;
        int st1;
        fscanf(ptr, "%d", &st);
        fscanf(ptr, "%d", &st1);
        fclose(ptr);
        printf("The value of st is %d\n", st);
        printf("The value of st is %d\n", st1);
    }
    return 0;

    // ptr=fopen("m.txt2","w"); // For writing the file
}*/

// For writing the file

