#include<stdio.h>
int main()
{
    printf("Hello World \n");
    return 0;
}

//#include<stdio.h>
//int main()
//{
    // Q1.Using c program calculate the area of a rectangular

    // int length=3,breadt=3;
    // int area=length*breadt;
    // printf("The value of rectangular is %d",area);


    // If we want to take input from user side then we will use scanf() function....
//   int length,breadt;
//   printf("What is the lenght of the rectangular ? \n");
//   scanf("%d",&length);
//   printf("What is the breadth of the rectangular ? \n");
//   scanf("%d",&breadt);
//   printf("The value of rectangular is %d",length*breadt);

// Q2.Calculate the area of a circle and modify the same program to calculate the volume of a cylinder given its radius and height

// Formula >> Area of circle is >> pi*radius*radius and Volume of a cylinder is >> pi*radius*radius*height

// int radius=3;
// float pi=3.14;
// printf("The area of circle is %f",pi*radius*radius);

// Q3.Write a c program to convert celsius to fereheit

// float celcius=37,far;
// far=(celcius*9/5)+32;
// printf("The value of the celcius temp in fehrenheit is %f",far);

// Q4.Write a c program to calculate the simple interest for a set of values representing principle ,number of years,rate of interest
 
//  int principal,rate,years;
//  printf("Enter the value of principle \n");
//  scanf("%d",&principal);
//  printf("Enter the value of Years \n");
//  scanf("%d",&years);
//  printf("Enter the value of rate \n");
//  scanf("%d",&rate);
//  printf("The value of simpleinterest is %d",(principal*years*rate)/100);
//return 0;
//}